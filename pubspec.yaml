name: dnyanjyoti_abhyasika_app
description: "Dnyanjyoti Abhyasika Admin App"
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  image_picker: ^0.8.4+4
  supabase_flutter: ^2.8.4
  flutter_dotenv: ^5.0.2
  url_launcher: ^6.0.20
  intl: ^0.17.0
  provider: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
